{
  "name": "treejer-second",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ estainit/treejer-second",
    "create-local": "graph create --node http://localhost:8020/ estainit/treejer-second",
    "remove-local": "graph remove --node http://localhost:8020/ estainit/treejer-second",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 estainit/treejer-second"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
